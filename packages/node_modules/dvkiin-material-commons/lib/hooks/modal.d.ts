export declare type ModalState<TData> = {
  isOpen: boolean;
  data: TData | undefined;
};
export declare type ModalStates<TData = any> = {
  [key: string]: ModalState<TData>;
};
export declare function useModal<TData = any>(): {
  isOpen: boolean;
  data?: TData;
  open: (data?: TData) => void;
  close: () => void;
};
export declare function useModals(modalNames: string[]): {};
