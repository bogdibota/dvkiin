import { FC, ReactNode } from 'react';
export declare type InfoModalProps = {
  open: boolean;
  onClose: () => void;
  message?: ReactNode;
  title: ReactNode;
};
declare const InfoModal: FC<InfoModalProps>;
export default InfoModal;
