import { FC, ReactElement, ReactNode } from 'react';
import { DVKField, DVKObject } from '../Form/domain';
export declare type InputModalProps = {
  open: boolean;
  title: string;
  formKey?: string;
  saveLabel?: string;
  onClose: () => void;
  onCreate?: (obj: DVKObject) => void;
  onChange?: (obj: DVKObject) => void;
  fields: DVKField[];
  defaultValue?: DVKObject;
  invalidFields?: {
    [key: string]: boolean | string;
  };
  renderActions?: (formId: string) => ReactElement | null;
  bottomContent?: ReactNode;
};
declare const InputModal: FC<InputModalProps>;
export default InputModal;
